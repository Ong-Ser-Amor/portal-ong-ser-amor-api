/**
 * Enum que define o veredito pedagógico do aluno ao encerrar a turma.
 * Aplicado apenas em turmas que possuem critérios de corte (nota/presença).
 */
export enum ResultadoFinalMatricula {
  APROVADO = 'APROVADO',
  REPROVADO = 'REPROVADO',
}
